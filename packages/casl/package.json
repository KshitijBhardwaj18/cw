{
	"name": "@repo/casl",
	"version": "0.0.0",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"private": true,
	"scripts": {
		"build": "rm -rf dist && tsc --build --force tsconfig.json",
		"dev": "rm -rf dist && tsc --build --force ./tsconfig.json --watch",
		"package": "bun run build"
	},
	"dependencies": {
		"@repo/db": "*",
		"@repo/shared": "*"
	},
	"peerDependencies": {
		"react": ">=18.0.0"
	},
	"devDependencies": {
		"@types/react": "*",
		"react": "*",
		"typescript": "*"
	}
}
