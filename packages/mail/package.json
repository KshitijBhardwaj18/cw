{
	"name": "@repo/mail",
	"version": "0.0.0",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"default": "./dist/index.js"
		}
	},
	"private": true,
	"scripts": {
		"build": "rm -rf dist && tsc --build --force",
		"dev": "tsc --build --watch",
		"typecheck": "tsc --noEmit",
		"package": "rm -rf dist && tsc --build --force"
	},
	"dependencies": {
		"nodemailer": "^8.0.1"
	},
	"devDependencies": {
		"@repo/typescript-config": "*",
		"@types/nodemailer": "^7.0.11",
		"typescript": "*"
	}
}
