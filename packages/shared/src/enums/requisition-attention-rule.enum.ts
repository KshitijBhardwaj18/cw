/** Mirrors Prisma `RequisitionAttentionRuleKey`. Keep synchronized with schema.prisma. */
export enum RequisitionAttentionRuleKey {
	SLOW_TIME_TO_FILL = "SLOW_TIME_TO_FILL",
	LOW_SUBMISSION_COUNT = "LOW_SUBMISSION_COUNT",
	NO_SUBMISSIONS = "NO_SUBMISSIONS",
}

/** Mirrors Prisma `RequisitionAttentionRuleUnit`. Keep synchronized with schema.prisma. */
export enum RequisitionAttentionRuleUnit {
	HOURS = "HOURS",
	DAYS = "DAYS",
	COUNT = "COUNT",
}
