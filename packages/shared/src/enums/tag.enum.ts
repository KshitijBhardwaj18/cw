/** Mirrors Prisma `TagType`. Keep synchronized with schema.prisma. */
export enum TagType {
	SKILL = "SKILL",
	COMPLIANCE = "COMPLIANCE",
	AVAILABILITY = "AVAILABILITY",
	PRIORITY = "PRIORITY",
	FLAG = "FLAG",
}
